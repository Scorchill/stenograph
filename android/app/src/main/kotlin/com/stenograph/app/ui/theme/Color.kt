package com.stenograph.app.ui.theme

import androidx.compose.ui.graphics.Color

// Primary action colors
val MicIdleBlue = Color(0xFF4A90D9)
val MicActiveRed = Color(0xFFE04848)

// Connection status — brightness-differentiated for color-blind accessibility
val ConnectedGreen = Color(0xFF66FF66)   // Bright / high luminance
val DisconnectedGray = Color(0xFF666666) // Dim / low luminance

// Surface / background accents
val PreviewBackground = Color(0xFF1E1E1E)
val PreviewText = Color(0xFFE0E0E0)
